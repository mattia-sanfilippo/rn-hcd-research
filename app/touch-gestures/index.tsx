import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const TouchGesturesScreen = () => {
    return (
        <SafeAreaView>
            <Text>Touch Gestures Screen</Text>
        </SafeAreaView>
    );
};

export default TouchGesturesScreen;