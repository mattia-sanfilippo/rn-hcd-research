import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const ThemingScreen = () => {
    return (
        <SafeAreaView>
            <Text>Theming Screen</Text>
        </SafeAreaView>
    );
};

export default ThemingScreen;