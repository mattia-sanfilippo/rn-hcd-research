import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const UserFeedback = () => {
    return (
        <SafeAreaView>
            <Text>User Feedback Screen</Text>
        </SafeAreaView>
    );
};

export default UserFeedback;