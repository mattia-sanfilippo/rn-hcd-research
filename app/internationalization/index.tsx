import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const InternationalizationScreen = () => {
    return (
        <SafeAreaView>
            <Text>Internationalization Screen</Text>
        </SafeAreaView>
    );
};

export default InternationalizationScreen;